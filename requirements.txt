# --- 移除冲突版本，使用最新的稳定版本 ---

streamlit
pydantic==1.10.17 
langchain==0.1.20
langchain-core==0.1.52

# 关键修正 1: 移除 langchain-google-genai 的错误版本号 (0.0.12 不存在)
# 并使用最新的稳定版本 (例如 0.0.12 之后，最新的是 0.0.11 或 0.0.13, 0.0.14)
# 🚨 我们可以使用 0.0.13 或直接使用最新版本来避免冲突
langchain-google-genai==0.0.13 

# 核心子包 (保持不变)
langchain-community==0.0.38
langchain-text-splitters==0.0.2

chromadb
pypdf
sentence-transformers
